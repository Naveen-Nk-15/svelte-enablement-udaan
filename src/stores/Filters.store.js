import { writable } from 'svelte/store';

export const oneWayTimeFilter = writable([]);

export const roundWayTimeFilter = writable([]);

export const priceRange = writable([1000,20000]);

export const flightCompanies = writable([]);

